EVM Periods tab now has PERIODS / CUMULATIVE views (Cash Flow pattern): entry on one side, a fully automatic read-only cumulative table on the other, each ending with a TOTAL row after the last period. The Cash Flow ledger gets the same TOTAL row (planned + actual sums, variance, net)
