Package Lag alerts: amber source card + Baseline tab mark when a newer source version is approved after the package (V1 to V2), and when all sources are approved but no package exists; clears itself once the new package is approved