Fix in CVR: the direct basis is now truly cumulative — the per-period direct components are accumulated over history exactly like the EVM class table, then divided by the direct BAC from the approved package. Previously a cumulative total was mixed with a per-period indirect slice, producing non-cumulative numbers. Direct basis engages whenever an approved split package exists
