Separate approvals: Direct and Indirect approve independently per period — the Total approves (and freezes) only when both are signed; reopening one class keeps the other standing and never erases data. Calendar sync no longer deletes periods that fall off a new cadence/schedule grid: entered drafts, approvals and frozen snapshots all survive
