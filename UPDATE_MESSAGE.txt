Trend tab EAC/VAC fixed: every row (table + forecast charts) now answers from its own cumulative performance with the official EAC method and today's BAC — same formula as the dashboard headline — instead of the approval-time frozen forecast that mixed methods and baselines across rows
