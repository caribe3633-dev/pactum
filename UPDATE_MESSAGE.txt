The Direct / Indirect class total row in the EVM Tables tab is restyled: a soft-gold footer band with a stronger top border, a two-line label (TOTAL plus the class name and that it spans all periods), white tabular figures, and the empty trailing span now explains itself
