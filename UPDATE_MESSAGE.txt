Cost assessment in BOTH Change Orders and Claims now links to the budget per class: a direct link to a direct-classified budget line and an indirect link to an indirect-classified line. The baseline rebuild adds only the unlinked class, per half — legacy rows with the old single link keep their exact behaviour
