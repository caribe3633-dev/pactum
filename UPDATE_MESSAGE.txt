Fix: the Indirect Cost Impact input is back in the cost assessment panel of both Change Orders and Claims (it was accidentally removed with the old single budget-line link). Direct input, indirect input, and the two per-class links (direct/indirect) all sit together in the panel again
